package app.service;

import org.springframework.stereotype.Service;

@Service
public class CollectionService {


    // createCollection(uuid userId, string name);
    // deleteCollection(uuid collectionId);
    // getCollection(uuid id);
    // list<collection> getCollectionForUser(uuid userId);
}
