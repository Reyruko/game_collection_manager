package app.service;

import org.springframework.stereotype.Service;

@Service
public class CollectionGameService {

    // void addGameToCollection(UUID collectionId, UUID gameId);
    // void removeGameFromCollection(UUID collectionId, UUID gameId);
    // List<Game> getGamesInCollection(UUID collectionId);
}
